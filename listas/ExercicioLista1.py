# Escreva um programa que some todos os itens de uma lista.

lista = [2, 4, 6, 8, 10]

soma = 0 #variável para armazenar a soma

for item in lista:
    soma += item

print("A soma de todos os itens da lista é:", soma)